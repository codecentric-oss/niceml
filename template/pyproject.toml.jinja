[tool.poetry]
name = "{{name}}"
version = "{{version}}"
description = "{{description}}"
authors = {{authors}}

[tool.poetry.dependencies]
{{dependencies}}
altair = {version = "^4.2.2", optional = true}
tensorflow-macos = {version = "~2.12.0", optional = true}
streamlit = {version = "^1.22.0", optional = true}
tensorflow = {version = "~2.12.0", optional = true}
tensorflow-metal = {version = "~1.0.1", optional = true}
tensorflow-io-gcs-filesystem = {version = "~0.32.0", optional = true}

[tool.poetry.extras]
tensorflow = ["tensorflow"]
tensorflow-windows = ["tensorflow", "tensorflow-io-gcs-filesystem"]
tensorflow-macos = ["tensorflow-macos", "tensorflow-metal"]
visu = ["altair", "streamlit"]

{% if use_precommit %}
[tool.poetry.group.dev.dependencies]
pytest = "^7.3.1"
pre-commit = "^3.3.1"

[tool.ruff]
line-length = 100

{%endif%}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
